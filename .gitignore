.DS_Store
.env
**/.env
.venv/
**/.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/

# Vendor clone — run locally via docker compose (see agent/README)
# The Medplum monorepo is 302MB — fetch it with ./scripts/bootstrap_vendor.sh instead.
# haarf/ and open-wearables/ ARE committed; see infra/README.md for provenance.
infra/medplum/

# Clone metadata moved aside so vendored trees commit as plain files, not gitlinks.
.vendor-git-backup/

# OAuth tokens + pending CSRF states for the connected Whoop strap — never commit
agent/.whoop_tokens.json
agent/.whoop_states.json

# Web
web/node_modules/
web/dist/
web/.env
.gstack/
